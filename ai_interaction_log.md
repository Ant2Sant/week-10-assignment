# AI Interaction Log

### Task: Page Setup and API connection
**Prompt:** Build Part A of the Streamlit app: use st.set_page_config(page_title='My AI Chat', layout='wide'), load the Hugging Face token from st.secrets['HF_TOKEN'], send one hardcoded test message ('Hello!') to the Hugging Face API, display the model response, and handle missing token, invalid token, rate limits, and network errors with user-visible messages instead of crashes.
**AI Suggestion:** The AI created a minimal app.py that reads the Hugging Face token from Streamlit secrets, sends a single test prompt to the Hugging Face Inference API with requests, displays the reply in the main page, and shows clear st.error() messages for missing secrets, empty tokens, API errors, and connection problems.
**My Modifications & Reflections:** The code worked as a clean Part A starting point and matched the assignment requirement not to hardcode the token. I kept the UI simple with native Streamlit components only and verified the file compiled before running it in Streamlit.

### Task: Multi-Turn Conversation
**Prompt:** Extend Part A into a real chat interface using native Streamlit chat components. Replace the hardcoded test message with st.chat_input(...), render messages with st.chat_message(...), store the full conversation in st.session_state, send the full history with each Hugging Face API request for context, and keep error handling user-visible instead of crashing
**AI Suggestion:** The AI rewrote app.py into a chat-based Streamlit app that initializes a session message list, displays the conversation with st.chat_message, collects new input with st.chat_input, builds a prompt from the full history, and sends that history to the Hugging Face Inference API so replies can stay context-aware across multiple turns.
**My Modifications & Reflections:** The code worked as a good Part B upgrade and stayed within the assignment rules by using only native Streamlit UI elements. I kept the conversation state in st.session_state, used a scrollable container for the chat history, and made sure the input stayed available at the bottom while preserving the earlier API error handling.

### Task: Chat Management
**Prompt:** Add Part C multi-chat support to the Streamlit app. Use st.sidebar for chat navigation, add a New Chat button, show a scrollable list of chats with title and timestamp, visually highlight the active chat, allow switching between chats without losing history, and add a delete button for each chat that safely removes it and falls back to another chat or an empty state.
**AI Suggestion:** The AI refactored the app to store chats as separate conversation objects in st.session_state, each with its own title, timestamp, and message list. It added a sidebar-based navigation system with native Streamlit buttons for creating, selecting, and deleting chats, while keeping the main chat interface connected to whichever chat is active.
**My Modifications & Reflections:** The code worked as a solid Part C extension and stayed within the assignment rules by using only native Streamlit components. I adapted the state model from one chat history into a multi-chat structure, used the first user message to help label chats, and kept the UI behavior simple so chat switching and deletion felt predictable during testing.

### Task: Chat Persistence
**Prompt:** Add Part D persistence so each chat session is saved as a separate JSON file in the chats/ directory. On startup, load all saved chat files into the sidebar automatically, allow continued conversation in previously saved chats, and when a chat is deleted from the sidebar, delete its corresponding JSON file from disk too.
**AI Suggestion:** The AI refactored the chat app to persist each conversation as an individual JSON file containing the chat ID, title, timestamps, and full message history. It added startup loading from the chats/ folder, automatic saving after message updates, and file deletion when a chat is removed from the sidebar.
**My Modifications & Reflections:** The code worked as a strong Part D upgrade because it kept the existing multi-chat interface while adding real persistence across app restarts. I adapted the session-state chat structure to sync with JSON files on disk, used stored timestamps and titles for the sidebar, and kept the save/load/delete logic simple enough to test clearly in Streamlit.

### Task: Response Streaming
**Prompt:** Add streaming to the chat app so the model reply appears token-by-token instead of waiting for the full response. Use stream=True with the Hugging Face API request, handle server-sent event data: lines, update the Streamlit chat UI incrementally as chunks arrive, and save the final full streamed reply back into chat history when streaming is complete.
**AI Suggestion:** The AI updated the Hugging Face request to use SSE streaming, parsed incoming JSON chunks from response.iter_lines(...), and rendered the assistant reply live in the chat area with a placeholder that updates as new text arrives. It also added a short delay between chunks so the streaming behavior stays visible even when the model responds very quickly, and then saved the completed response into the persistent chat history.
**My Modifications & Reflections:** The code compiled successfully and fit the assignment by using native Streamlit updates instead of custom front-end code. I adapted the chat flow so the user message appears immediately, the assistant response streams inside the chat interface, and the final combined text is still stored correctly in the saved chat JSON files.

### Task: User Memory
**Prompt:** Add persistent user memory to the Streamlit chat app. After each assistant response, make a second lightweight API call that extracts personal traits or preferences from the latest user message as JSON, store that data in memory.json, display it in a sidebar expander with a native clear/reset control, and inject the saved memory into future prompts so responses become personalized.
**AI Suggestion:** The AI added a memory pipeline that loads and saves memory.json, sends a second Hugging Face request to extract user traits from the latest message, merges those traits into persistent memory, displays the saved memory in a sidebar expander, and includes the memory JSON in the system prompt for future responses so the model can personalize its replies.
**My Modifications & Reflections:** The code worked as a strong memory feature extension and stayed within the assignment by using native Streamlit controls for display and reset. I adapted the prompt-building logic so saved traits affect future responses, kept the memory format simple as a JSON dictionary, and preserved persistence so memory survives app restarts along with the saved chats.


App is found through "justchatting.streamlit.app"